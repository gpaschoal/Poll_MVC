@model IEnumerable<Poll>

@{
    ViewData["Title"] = "Index";
}

<a class="btn btn-success" asp-controller="Poll" asp-action="Create">Novo</a>

<h1>Polls</h1>
<table class="table">
    <thead>
        <tr>
            <th scope="col">Id</th>
            <th scope="col">Description</th>
            <th scope="col">Views</th>
            <th scope="col">Detail</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <th scope="row">@item.Id</th>
                <td>@item.Description</td>
                <td>@(item.Views.Count)</td>
                <td>~SomeAction</td>
            </tr>
        }
    </tbody>
</table>